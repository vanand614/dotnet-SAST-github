name: DotNet SAST

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  codeql:
    uses: vanand614/security-platform/.github/workflows/reusable-codeql-dotnet.yml@main

    with:
      project-path: ./app/SampleWebApi/SampleWebApi.csproj